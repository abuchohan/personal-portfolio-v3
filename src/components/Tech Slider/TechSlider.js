import Title from '../UI/Title'

import classes from './TechSlider.module.scss'

const TechSlider = () => {
  return (
    <Title hasOverline={true} tag={'h2'}> Title </Title>
  )
}
export default TechSlider